{
  "name": "hld-reel",
  "private": true,
  "type": "module",
  "description": "Marketing reel renderer. Deliberately its own package so Playwright never lands in the product's dependency tree.",
  "scripts": {
    "beats": "node shoot.mjs --beats",
    "reel": "./make.sh"
  },
  "dependencies": {
    "playwright": "^1.62.1"
  }
}
