[project]
name = "hld-playground"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pipecat-ai[openai,silero,webrtc,local-smart-turn]==1.7.0",
    "fastapi>=0.115",
    "uvicorn>=0.34",
    "python-dotenv>=1.0",
]
