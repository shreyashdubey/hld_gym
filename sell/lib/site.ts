/* One origin, named once. layout.tsx, robots.ts, sitemap.ts and the structured
   data all need it absolute: scrapers and crawlers do not resolve relative URLs
   the way a browser does. */
export const SITE = "https://hld-gym.vercel.app";
